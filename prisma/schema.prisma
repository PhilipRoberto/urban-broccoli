generator client {
  provider = "prisma-client"
  output   = "../db/generated"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @unique
  name      String
  email     String
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime
}
